org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
# Release signing values are supplied during configuration, so keep the
# configuration cache disabled unless a non-release developer build opts in.
org.gradle.configuration-cache=false
org.gradle.parallel=true
android.useAndroidX=true
